node_modules/
.env
.env.local
.env.*.local
.DS_Store
*.log
dist/
build/
*.tmp
.vscode/
.idea/
*.swp
*.swo
.broadcast-state.json

# 晚间静默积压文件（运行时状态，勿提交）
.quiet-backlog.json

# Excel 打开工作簿时生成的锁文件（~$xxx.xlsx），勿提交
~$*

# 私有覆盖（可含真实成员姓名，仓库只进脱敏模板 *.json）：
# 关键词回答 autoReplies / @触发回答 autoRepliesMention / 抽奖 lottery —— 通配防漏（2026-09-17）
server/src/config/*.local.json
